#!/bin/bash

set -euo pipefail
set -x

probe-packages > PACKAGES.txt
probe-libs     > LIBS.txt
rm -vfr local/man local/cache
tar --transform "s@^@$NAME/@" \
    -zcf /vendor.tar.gz \
    PACKAGES.txt \
    LIBS.txt \
    cpanfile \
    cpanfile.* \
    local \
    vendor
